# This file is an example. For production, you may want to use a .env file
# to specify the path to your Google Cloud service account credentials.
# Make sure your .gitignore file includes .env and *.json to keep these secret.

# Path to your Google Cloud service account JSON key file.
# This is the recommended way to authenticate for a production application.
# GOOGLE_APPLICATION_CREDENTIALS=./path/to/your-service-account-key.json
